# To Do

Not ordered/prioritized — pull from whichever's relevant.

## Content

- [x] ~~Real Music section~~ — rebuilt 2026-08-31 with a real YouTube IFrame Player API integration + custom track list (replacing the earlier fixed `?playlist=` chain), see `DESIGN_NOTES.md`. Track captions are a mix of our own visual descriptions and Doug's own corrections (real song titles — "Cantaloupe Island", "Isn't She Lovely") for the clips he actually knows the content of; two clips (`KGx6Zwm1Dsg`, `-4trKHWWudA`) and the one never-successfully-watched clip (`3W4F4VmI0tQ`) are staying as-is for now per Doug ("don't worry too much about those"). No Spotify link yet — add if/when the band has music on Spotify.
- [x] ~~Real Shows section~~ — built (`src/components/Shows.astro`, ticket-stub list grouped by month). As of PR #7, `src/data/shows.ts` fetches real gigs from the GigSync backend at build time (falls back to placeholder dates if unreachable) — see `DESIGN_NOTES.md`. Real data is already flowing: one confirmed gig as of 2026-08-31 (Constellation, Nov 20). GigSync has no ticket-URL field, so those shows now show an "Ask about tickets" link to `#book` instead of a dead-end label — fixed alongside a real contrast failure and touch-target failure on that same element, see `QUALITY_CHECKLIST.md`.
- [x] ~~Booking section~~ — built (`src/components/Booking.astro`), real `mailto:` with pre-filled subject. `hello@guacamayoband.com` is a **placeholder inbox**, not live — swap once a real address exists. A real form still needs a backend/service like Formspree if wanted later.
- [ ] Photo gallery — blocked on having real photos beyond the flyer/logo; not faking this one with placeholder images
- [x] ~~Press/EPK section~~ — built (`src/components/Press.astro`): pitch line, contact, and real downloadable logo/flyer (original quality, served from `public/press/`, not the resized web versions). No press quotes/reviews included — none exist yet, not inventing any. Add real ones as they come in.
- [x] ~~About/bio~~ — built (`src/components/About.astro`), placed right after Hero. Bio copy is now a **second draft using only verified facts** (lineup, instruments, genre, city, the real songs the band covers, established setting language) — longer and more specific than the first pass, still no invented backstory. Swap for the band's own voice before launch.
- [ ] Confirm exact band-member spelling/instrument credits against the flyer before publishing (Doug Rosenberg/sax, Jack Zará/bass, Manuel Reyes/guitar, Javier Saume Mazzei/drums)

## Build

- [x] ~~Shows section crashing on a bad GigSync date~~ — the gigsync-integration session logged a gig with an unparseable source date ("32-September,2026"); GigSync stores that as `date: ""` rather than guessing, and `Shows.astro` fed that straight into `Intl.DateTimeFormat`, which throws on an invalid date and took down the entire homepage build. Fixed by filtering out gigs with an empty/unparseable date in `shows.ts` before they reach `Shows.astro` (logs a warning naming the venue so it's not silently dropped) — same "one bad record can't break the build" resilience already used for a full fetch failure, just scoped to a single bad item. The Reginalds gig itself still needs a real date from Doug before it'll show up.
- [x] ~~Mobile nav~~ — nav is now fixed/global with a real logo (2026-08-31), which pushed it past fitting at 375px on the original spacing. Tightened mobile-only sizing to fit comfortably ≥375px; `overflow-x-auto` now genuinely earns its keep below that (a few px short at 320px). Verified via direct DOM measurement of the real mobile CSS values, not the visual resize tool — `resize_window` wasn't actually changing the rendered viewport in this environment tonight, worth knowing if it recurs.
- [x] ~~Real hamburger/expanding mobile nav menu~~ — the `overflow-x-auto` tradeoff above wasn't actually solid; the "Book" tile could overflow past the edge with a genuine horizontal scrollbar on a narrow viewport. Below 640px the tile row is now a hidden dropdown panel toggled by a standard hamburger button (morphs to an X via `aria-expanded`), closing itself on tile click; at 640px+ it's back to the original always-visible inline row, no scroll fallback needed there. See `DESIGN_NOTES.md` for the breakpoint verification method (`resize_window` was unreliable again tonight — used a fixed-width iframe instead).
- [x] ~~"Book" still bled past the edge at real narrower-desktop widths + rotateZ skew rejected~~ — the desktop row above needed `overflow-x: auto` back (removing it also removed a load-bearing flexbox side effect, not just a "just in case" fallback — see `DESIGN_NOTES.md`), verified this time across a swept range of widths, not one spot-check. Separately, direct feedback rejected the `rotateZ` skew for "a fun flip animation" — rebuilt as a real front/back `rotateY` flip card (grid-stacked faces per the Smashing Magazine "magic flip card" technique), with a checkmark + consistent paper2/ink "activated" face and an ink ring so every tile's active state stays visually distinct from its own resting colors (three tiles would otherwise invert to ink-on-ink, invisible against the nav's own ink background). See `DESIGN_NOTES.md`.
- [x] ~~Last-tile hover scrollbar (again) + flip axis/trigger redesign + global smooth scroll~~ — the `scale()` hover on the last tile grew its box past the row's edge and surfaced a real horizontal scrollbar; dropped `scale()` from hover entirely (brightness alone doesn't touch layout). Direct feedback also rejected the Y-axis spin for the site's own established `rotateX` flap motion (matching `Shows.astro`'s date tiles) and asked for the flip to trigger on hover, not click — which let the whole JS click-tracking/`.is-active` system go: the flip is now pure `:hover`/`:focus-visible` CSS, reverting the instant the pointer leaves. Added `scroll-behavior: smooth` globally (`prefers-reduced-motion`-gated) so anchor-jump nav reads as a scroll, not a cut. See `DESIGN_NOTES.md`.
- [x] ~~Hover clipped/glitched at tile edges + no "current section" indicator~~ — a real rendering bug: `.tile-face`'s `border-radius: inherit` was inheriting from `.tile-flipper` (never set), not `.nav-tile`, so faces rendered square while the outer tile was rounded — the mismatch showed as a seam mid-flip. Fixed with explicit matching radii plus `overflow: hidden` on the non-transformed outer box. Separately, the hover-only flip redesign above had dropped the only "where am I" indicator along with the JS click-tracking it replaced — added back a real `IntersectionObserver` scroll-spy (independent of hover) that marks the nav tile matching the currently-centered section with a small growing underline. See `DESIGN_NOTES.md`.
- [x] ~~Flipped face still clipping (for real this time) + checkmark removed~~ — the radius fix above wasn't the actual cause: the back face's "✓ Label" text needed 59px but the grid-stacked tile only rendered it at 55px, a real CSS-Grid sizing shortfall (not fully root-caused — see `DESIGN_NOTES.md`). Fixed by removing the checkmark itself (agreed it wasn't adding anything the paper2/ink face + ring didn't already say) so both faces share identical text and can't mismatch in width again. Verified all five tiles' front/back widths now match exactly.
- [x] ~~Tiles reading too small horizontally after the checkmark removal~~ — dropping the checkmark shrank every tile back to its bare-label width. Went through a few iterations landing on padding of `24px`/`26px` (mobile/desktop) per side — but every one of them was on `.nav-tile`, which is `background: transparent`; the actual color lives on `.tile-face`, sized independently to just its text. Padding on `.nav-tile` was only ever adding invisible space *around* a colored box that stayed text-width — never actually widening the visible tile, which is what was being asked for each time. Fixed by moving the padding to `.tile-face` itself (`padding-inline: 24px` / `26px` at `sm`), confirmed with `tile.getBoundingClientRect().width === face.getBoundingClientRect().width` — the colored box is now genuinely the full padded width. Applies to all five tiles uniformly (one shared class, no per-tile styling). Same safety net as before: the desktop row's `overflow-x: auto` still only needs to kick in for a narrow ~645–680px window range, never page-level bleed.
- [x] ~~Underline retired, glassy resting state + glow, flip plays on scroll~~ — direct feedback didn't like the underline and asked for a small element besides opacity; offered a soft color glow as the recommendation (asked via `AskUserQuestion`), picked. Resting tiles are now translucent (`color-mix` on just the background, not the whole face, so text stays crisp) plus `backdrop-filter: blur`; the current tile drops the blur, goes fully solid, and gets `filter: brightness + drop-shadow` (drop-shadow specifically, since `box-shadow` would get clipped by `.nav-tile`'s own `overflow: hidden`). Also added a one-shot flip-pulse on scroll (ties into the same flap-board motif the Shows tiles already use), deduped so revisiting an already-current section doesn't replay it, skipped under `prefers-reduced-motion`. See `DESIGN_NOTES.md`.
- [x] ~~Real double-flip bug in the scroll pulse + more glassiness~~ — the pulse used a separate `@keyframes` animation that fought with `:hover`'s own `transition` on the same `transform` property whenever the pointer was resting on the tile as the pulse ended (a common case, since scrolling doesn't move the cursor) — verified with direct `getComputedStyle` sampling through a real hovered pulse, confirmed fixed after. Rebuilt the pulse to just set the same `rotateX(180deg)` target hover already uses via the same transition, no competing animation. Also bumped glassiness further: `color-mix` 65%→55%, blur 6px→8px. See `DESIGN_NOTES.md`.
- [x] ~~Current tile stuck forever once set~~ — a screenshot showed the page scrolled to the very top with "Music" still glowing as current, even though the URL/nav said `#about`. Root cause: the scroll-spy only ever added `.is-current`, never removed it when a section exited the observed band with nothing else entering (e.g. scrolled back above every section into the Hero). Fixed by handling the exit case too, verified with a `MutationObserver` class-change log through a full scroll-down-then-back-to-top pass: every section pulsed exactly once, and nothing stayed stuck at the top afterward. Separate bug from the animation/transition double-flip above, not the same cause. See `DESIGN_NOTES.md` (also notes a real `IntersectionObserver`-stopped-firing environment dead end hit while verifying this, resolved by a fresh tab group — not a site bug).
- [x] ~~Double-flip, three rounds of fixes~~ — persisted through the animation/transition fix and the stuck-tile fix; the third cause (clicking a tile, pointer drifting off before the scroll-spy's own pulse fires) was fixed with click-suppression, though automation couldn't confirm that one end-to-end (coordinate-scale + synthetic-click limitations, see `DESIGN_NOTES.md`). Made moot anyway by the flip redesign below: hover and `.is-current` now drive one unified CSS-only `rotateX(180deg)` target, no separate JS-timed pulse left to ever race against anything — structurally can't double-flip regardless of trigger.
- [x] ~~Carnival palette shipped + backdrop brightened~~ — picked from a real 4-option side-by-side comparison (published as an artifact, also saved to `ui-lab`). `tokens.css` rewritten; `--color-paper`/`--color-rust` dropped (confirmed genuinely unused via a full `src/` grep, not just replaced). Re-ran the real WCAG contrast script against the new hexes rather than assuming old results held — Book's tile needed ink text instead of paper2 as a result (paper2-on-new-red only hits 3.38:1). Backdrop filter dropped `sepia`/`brightness(.6)` entirely for `saturate(1.4) contrast(1.08) brightness(1)` per feedback that the real YouTube thumbnails read as much brighter than the page backdrop. See `DESIGN_NOTES.md`.
- [x] ~~Nav flip: white face nixed, flip retained with a different destination~~ — went through dropping the flip mechanic entirely (shipped, then corrected: "retain the flip, just not the white state"). Rebuilt as a real front/back `rotateX` card again, back face now the tile's own color at `brightness(1.3)` instead of paper2/ink, glow via `filter: drop-shadow` on `.nav-tile` (not the back face, which would get clipped by the tile's own `overflow: hidden`). Hover and `.is-current` share the identical target. `ease-in-out` throughout per direct request. See `DESIGN_NOTES.md`.
- [x] ~~Nav bar translucency + bigger logo, previewed live before committing~~ — `bg-ink/40` → `/20` → `/10` shown via real screenshots at each step rather than described; logo `h-6 sm:h-9` → `h-9 sm:h-12`. Verified no overflow at 375px with the larger logo.
- [x] ~~Sections too vertically short for their own nav-matched tint to read, About specifically still looked brown~~ — bumped `py-16` → `py-24 sm:py-32` across all five sections uniformly, and gave About's own tint real strength (`bg-ink/85`+`bg-gold/8` → `bg-ink/70`+`bg-gold/25`) rather than just a whisper of color. Ink stays the dominant layer (70%) so text contrast stays in the same safe territory as before rather than needing a fresh numeric check; still covered by the standing WCAG-recheck item below, not duplicated. See `DESIGN_NOTES.md`.
- [x] ~~Same fix applied to Music, Shows, Press, Book~~ — same `ink/70`+`color/25` template as About, Book bumped bolder (`ink/60`+`red/35`) per more emphatic feedback. See `DESIGN_NOTES.md`.
- [x] ~~Shows: real Calendar tab alongside the List view~~ — for prospective musicians thinking in "am I free that week" terms rather than "what's the next show." Real month grid driven by the same show data, weekday alignment verified against actual known weekdays (not assumed), marked days get a native tooltip plus a compact legend list (not tooltip-only, which fails touch/most keyboard nav). See `DESIGN_NOTES.md`.
- [x] ~~Backdrop brightened past its own natural exposure~~ — the earlier pass only got back to `brightness(1)` (unaltered); asked to go further, now `brightness(1.15)`. Checked Hero's text legibility specifically since it relies on the gradient scrim alone, not a full glass wash — still comfortable at this level.
- [x] ~~Nav bar opacity reduced further~~ — `bg-ink/10` → `bg-ink/5`, third step down overall. Tiles keep their own color independent of the bar's tint, unaffected either way.
- [x] ~~"Book tab horizontal scroll" — real bug, earlier dismissal was wrong~~ — the row's own `overflow-x: auto` genuinely needed to scroll to reach Book at real widths (confirmed at 700px), which the tile-padding/logo-size increases made earlier tonight caused without anyone revisiting whether the 640px breakpoint still had room. Raised the row/hamburger breakpoint to a custom 780px (measured, not guessed) and swept the *entire* 375-1200px range this time — zero widths need the row's own scroll now. See `DESIGN_NOTES.md`.
- [x] ~~Navbar border removed — floating nav buttons~~ — self-aware direct ask ("I kind of like floating nav buttons, but realize this is weird"). Dropped `border-b border-paper-2/10` from `<nav>`; combined with the already-near-invisible `bg-ink/5`, logo/tiles/hamburger now read as genuinely floating over the backdrop. See `DESIGN_NOTES.md`.
- [x] ~~Scrollbar back after clicking Book — deeper cause than the breakpoint~~ — not a narrow-width issue this time: `scale(1.05)` on hover/focus/is-current inflates the row's scrollable-overflow region (a transform's ink bounds count toward the parent's scrollable overflow in most engines), overflowing the row's fixed 475px `clientWidth` at literally every tested width, 800-1536px. Removed `overflow-x: auto` from the ≥780px row entirely — the breakpoint fix already guarantees the resting content always fits, so that property was never protecting anything at this point, just creating the scrollbar risk. Re-verified across the full range. See `DESIGN_NOTES.md`.
- [x] ~~Scrollbar STILL back after removing `overflow-x: auto` — actual root cause~~ — the fix above only removed the *authored* rule; `.nav-tile-row`'s own unconditional `overflow-y: hidden` (an unrelated leftover guard) was silently forcing `overflow-x` back to its used value `auto` per the CSS overflow spec (one non-visible axis converts the other from `visible` to `auto`). Confirmed visually via screenshot (computed-style checks alone had missed it), removed `overflow-y: hidden` entirely — nothing constrains this row's height, so it was never actually guarding anything real. `overflow-x` now computes `visible` even with `.is-current` applied. See `DESIGN_NOTES.md`.
- [x] ~~Nav flip too fast~~ — every `300ms ease-in-out` transition in the flip/glow system bumped to `600ms ease-in-out`. See `DESIGN_NOTES.md`.
- [x] ~~"No brown!" — Press section reading as muddy olive, not cream~~ — the tile-matching recipe that worked for the four saturated-color sections (`ink/70`+`color/25`) doesn't work for a near-white neutral like `paper-2`; even `ink/85`+`paper-2/40` still computed to gray-brown. Inverted the treatment instead: `ink/45`+`paper-2/65`, the one genuinely light section on the page, with all its own text flipped from paper-2 to ink to stay readable. See `DESIGN_NOTES.md`.
- [x] ~~Site 20% brighter~~ — backdrop filter `brightness(1.15)` → `1.38`, gradient scrim scaled down to match (`/20`→`/15`, `/70`→`/55`), and each section's own `ink` overlay dropped ~20% relative (`/70`→`/55` About/Music/Shows, `/60`→`/50` Booking; Press exempted, just redone above). Accent-color layers and nav opacity left alone. See `DESIGN_NOTES.md`.
- [x] ~~Mobile scrolling/animations too fast (logo click from Booking especially)~~ — real bug underneath the complaint: the logo `<a>` had no click handler, and on the index page its href matches the current URL exactly, so a click was a full page reload, not a scroll. Fixed: logo click now intercepted (mobile only) with a real scroll-to-top; mobile dropdown menu now animates open/closed (`max-height`/`opacity`, not a `display` snap); nav-tile/logo clicks use a distance-scaled eased scroll on mobile instead of native `scroll-behavior: smooth` (desktop unchanged). Hit and fixed a real compounding-smoothing bug along the way (`window.scrollTo` needs explicit `behavior:"instant"` per-frame or the page's own smooth-scroll CSS fights the manual easing). See `DESIGN_NOTES.md`.
- [x] ~~Parrot moving independently of the rest of the backdrop~~ — no real cutout asset exists, so faked with two masked copies of the same `flyer.jpg` and amplified tilt on the "parrot" one. Explicitly an experiment to revert if it didn't work; verified it does (no gaps, real visible independent movement) — see `DESIGN_NOTES.md`. Renders the flyer image twice; worth revisiting once/if a real isolated parrot asset exists, folded into the image-optimization item below.
- [x] ~~Backdrop drift given a Z-axis rotation (±4deg)~~ — bare `rotate()` already is Z-axis rotation, no new property needed. Caught a real gap bug: the proposed `1.02`/`1.08` scale wasn't enough overscan for the rotation (confirmed visually — a black edge at top/bottom on wide viewports), raised to `1.24`/`1.3`. See `DESIGN_NOTES.md`.
- [x] ~~Backdrop 3D tilt on mouse move / scroll~~ — reference was a video showing a poster tilting in 3D depth as the camera moves, not a full spin. Built as `rotateX`/`rotateY` on the image (mouse position + scroll progress, rAF-throttled, 300ms ease-out), layered on a *separate* element from the existing Ken Burns drift animation (a CSS animation always beats an inline JS transform on the same element, so they'd fight if combined). See `DESIGN_NOTES.md`.
- [x] ~~Logo scroll still abrupt on desktop~~ — the mobile fix above wrongly gated the logo's new click handler to `isMobileNav()`; the underlying bug (same-URL reload, no hash to smooth-scroll to) was never mobile-specific, just not the originally reported scenario. Ungated for the logo specifically. See `DESIGN_NOTES.md`.
- [x] ~~Sections extremely left-justified, lots of dead space on wide screens~~ — content sits in `max-w-md`/`max-w-lg` blocks but sections never constrained/centered that, leaving the right two-thirds as flat tint over nothing at wide viewports. Fixed by fading each section's glass tint out left-to-right (`.section-glass-fade` mask-image, `global.css`) at `xl` (1280px)+ only, revealing the backdrop art on the right instead. Mobile/tablet unchanged (full flat tint, no room to spare). See `DESIGN_NOTES.md`.
- [x] ~~Real WCAG contrast pass on the glass sections~~ — fixed all 13 failures found in the earlier paused pass. Raised `ink` overlay opacity (Music `/55`→`/75`, Booking `/50`→`/72`, About `/55`→`/85`, Shows `/55`→`/76`); About's instrument label switched from `text-orange` to `text-paper-2/85` (orange never passed at any ink darkness — a hue/lightness problem, not a brightness one); Shows' month/city-time/unmarked-calendar-day text opacities raised (`/50`→`/68`, `/60`→`/78`, `/40`→`/68`) instead of just brute-forcing Shows' `ink` to near-solid. Re-verified all 13 originally-failing checks pass with real margin (smallest 4.73:1 vs. 4.5:1 required) and confirmed visually. See `DESIGN_NOTES.md`.
- [x] ~~Footer, for Booking's abrupt ending~~ — new `Footer.astro`, added to `index.astro` only (not `Layout.astro`/`404.astro` — the 404 page is a single full-viewport screen, not a long scroll that needs closure). Solid `bg-ink` (not a translucent glass panel like the sections above it) so the page gets real closure instead of the bold red Booking section just stopping mid-backdrop. Logo, tagline, the same five nav links/colors as `Nav.astro` (plain text, tinting to each link's color on hover — no flip mechanic, which would be redundant motion on links that already duplicate the nav above), copyright line. Not fixed/sticky. See `DESIGN_NOTES.md`.
- [x] ~~URL hash didn't update on scroll~~ — real gap, not a regression: the scroll-spy only ever toggled `.is-current`, never touched `location.hash`. Added `history.replaceState` (not `pushState`, not `location.hash =` directly) at the same set/clear branches already driving the glow state. `replaceState` mechanism verified directly (no scroll side effect); full observer-driven end-to-end check blocked by `IntersectionObserver` going dead again in the automation tab, not resolved by a reset this time — reasoned correct from code review, not confirmed live. See `DESIGN_NOTES.md`.
- [x] ~~`Music.astro`'s track list overflowing the page right above the 640px breakpoint~~ — flagged earlier tonight as pre-existing and out of scope, then turned out to be the real cause of a "Booking has a horizontal scrollbar" report (page-level scrollbars aren't scoped to whichever section you're looking at — see `DESIGN_NOTES.md`). Fixed with `min-w-0` on the track list `<ul>`, which had no override of a flex item's content-based automatic minimum width and refused to shrink far enough even though the `truncate` span one level down was already correctly set up to handle it once it got the chance.
- [x] ~~"Bottom half of the app" reading as a flat dark wall~~ — direct feedback: sections below the hero were opaque `bg-ink`, and the backdrop's sticky pin only ever covered one viewport's worth of scroll by design, so it was genuinely gone for the rest of the page. `PageBackdrop.astro` switched from sticky+negative-margin to `position: fixed` (stays behind the whole page automatically), and every section now sits on a translucent `bg-ink/85 backdrop-blur-md` glass panel tinted `8%` with that section's own nav-tile color instead of solid ink. See `DESIGN_NOTES.md` for the full reasoning and a flagged gap: contrast was checked visually, not with the hand-computed WCAG math used elsewhere in this project, since text no longer sits on a single flat known color.
- [ ] Real WCAG contrast re-check on the new glass sections (see gap noted just above and in `DESIGN_NOTES.md`) — checked visually this pass, not computed
- [ ] `backdrop-blur` is now applied across every section instead of just the nav bar, on top of a `position: fixed` backdrop image — not benchmarked for real scroll performance on lower-end/mobile hardware, worth a look before launch
- [ ] Image optimization pass on the flyer background specifically — check real mobile payload size once Music/Shows content exists (see the Blazor-vs-Astro payload comparison earlier in this project's chat history for why this matters)
- [ ] Decide whether to pull in `GuacamayouFirstVideo.mp4` (the real promo clip) anywhere — explored in `ten-video-backdrops.html` but not in the live build
- [x] ~~Favicon~~ — replaced with the real macaw-head icon, see `QUALITY_CHECKLIST.md`
- [x] ~~Broken favicon + Press download links in production~~ — `BASE_URL` doesn't end in a slash on this project; the favicon links (PR #4) and Press download links (PR #6) were silently malformed and 404ing live. Fixed with an explicit-slash join helper — see `DEPLOYMENT.md` lessons-learned.
- [x] ~~Open Graph/Twitter/og:image, robots.txt/sitemap.xml, custom 404, WCAG contrast audit, touch-target audit~~ — all built/fixed in one pass, see `QUALITY_CHECKLIST.md` for specifics on what was actually wrong and how each was verified (not just implemented and assumed correct)
- [x] ~~`astro.config.mjs` — `site` still has a placeholder `your-username` value~~ — fixed, set to `shalant.github.io`
- [x] ~~Sticky/persistent nav~~ — nav was only ever inside `Hero.astro`, scrolling away with it; moved to `Layout.astro`, `position: fixed`, glassmorphism (restrained, tied to the real palette — see `DESIGN_NOTES.md`), real logo linking home. All link hrefs now home-prefixed so they still work from `404.astro`.
- [x] ~~Colorful flap-tile nav links + hover/active/focus states~~ — explicit "max out the coloration" prototype pass, see `DESIGN_NOTES.md` for the full reasoning (contrast fixes, mobile-scroll tradeoff, hover scoped to real pointers only). If this becomes the final direction rather than a prototype, revisit the mobile horizontal-scroll tradeoff.
- [x] ~~Nav-tile hover scrollbar + interaction redesign~~ — direct feedback on the shipped prototype: hover was surfacing an unwanted vertical scrollbar (the `rotateX`+`perspective` 3D tilt combined with `overflow-x-auto` implicitly computing `overflow-y: auto` too). Replaced with a flat scale+brightness hover (no tilt), forced `overflow-y: hidden` on the tile row, and added a persistent click-to-activate state (`rotateZ` + a static ring that survives `prefers-reduced-motion`) — see `DESIGN_NOTES.md`.
- [x] ~~Split-flap tiles + subtle backdrop animation~~ — split-flap treatment (from `ten-flyer-variations.html`) applied to the real Shows date badges, flapping in once via IntersectionObserver, not looping. Backdrop now has a slow (28s) Ken Burns drift. Both `motion-safe:`/`prefers-reduced-motion`-aware. This also resolves the scroll-triggered-reveal item from the canonical checklist (`QUALITY_CHECKLIST.md` didn't call it out explicitly as deferred, but it was never actually built anywhere until now).

## Infra / deploy

- [x] ~~Decide GH Pages vs. Cloudflare Pages~~ — GH Pages to start (free, no domain needed yet); see `docs/DEPLOYMENT.md` for the Cloudflare move plan
- [x] ~~git init + first commit~~
- [x] ~~Push to GitHub, set up Pages~~ — live at <https://shalant.github.io/guacamayo-band/>, auto-deploys via `.github/workflows/deploy.yml` on merge to `master`
- [x] ~~All future changes must be made on a branch~~ — branch protection enforced on `master` (PR required, including for admins); PR template requires docs to be updated before merge
- [ ] Buy a domain, eventually — no rush, GH Pages subdomain works for now
- [ ] Once a domain exists: move to Cloudflare Pages per `docs/DEPLOYMENT.md` (confirms Brotli, HTTP/3, edge caching)

## Open decisions

- [ ] Waiting on Javier's vote on the overall direction — current build (Full-Bleed Pin) is Doug's pick, not yet confirmed with the rest of the band
- [x] ~~Whether to mix elements from other explored directions in~~ — split-flap tiles now integrated into Shows (2026-08-31), see above. Ticket-stub Shows layout is effectively already the chosen approach (the whole section is ticket-stub styled). Full "next show" flap strip in the Hero is still open — now that Shows data is live/build-time-fresh (not stale hardcoded info), the original "no specific show info in hero" concern from `DESIGN_NOTES.md` no longer fully applies; worth revisiting if wanted.
