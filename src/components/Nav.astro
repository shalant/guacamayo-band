---
const links = [
	{ label: "About", href: "#about" },
	{ label: "Music", href: "#music" },
	{ label: "Shows", href: "#shows" },
	{ label: "Press", href: "#press" },
	{ label: "Book", href: "#book" },
];
---

<nav
	class="relative z-10 flex items-center justify-between gap-4 px-6 pt-6 font-mono text-xs tracking-widest text-paper-2 uppercase sm:px-10 sm:pt-8"
>
	<span class="shrink-0">Guacamayo</span>
	<span class="flex gap-4 overflow-x-auto whitespace-nowrap sm:gap-8">
		{
			links.map((link) => (
				<a href={link.href} class="transition-opacity hover:opacity-70">
					{link.label}
				</a>
			))
		}
	</span>
</nav>
